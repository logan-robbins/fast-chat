# Chat API Environment Configuration

# Database Configuration
# PostgreSQL connection URL for thread and message persistence
# Format: postgresql+asyncpg://user:password@host:port/database
# Default: postgresql+asyncpg://localhost/chatdb
DATABASE_URL=postgresql+asyncpg://localhost/chatdb

# Enable SQL statement logging (True/False)
SQL_ECHO=False

# Backend chat-app service URL
CHAT_APP_URL=http://localhost:8001/v1/chat/completions

# Vector Store Configuration (ChromaDB)
# chat-api owns the vector store - chat-app calls /v1/search
# Empty = in-memory only (development)
# Set path for persistence (production)
CHROMA_PERSIST_DIR=./data/chroma

# OpenAI API Configuration (for embeddings and token counting)
OPENAI_API_KEY=

# Server Configuration
HOST=0.0.0.0
PORT=8000
